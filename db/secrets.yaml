kubectl create secret generic wordpress-secret \
  --from-literal=db-password=c091cfrgh \
  --namespace wp

kubectl create secret generic mariadb-secret \
  --from-literal=db-password=abcv1679 \
  --namespace db

